<!DOCTYPE html>
<html lang="en">
<head>
    <title>Profile page</title>
</head>
<body>
    <h1>Profile page</h1>
    <h3>Welcome <%= user.name %></h3>
    <ul>
        <% user.skills.forEach((item)=> { %>
        <li> <%= item %></li>
     <%   }) %>
    </ul>
</body>
</html>